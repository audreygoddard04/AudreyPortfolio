{
  "name": "audrey-portfolio-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-app run dev",
    "build": "npm --prefix my-app run build",
    "start": "npm --prefix my-app run start"
  },
  "dependencies": {
    "next": "16.3.5"
  },
  "workspaces": [
    "my-app"
  ],
  "engines": {
    "node": ">=22.12.0"
  }
}
